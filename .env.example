# Cloud / production only. Example:
# EXPO_PUBLIC_API_BASE_URL=https://api.homatri.com
#
# Leave this unset on a physical phone in Expo Go.
# The app then uses the same Wi-Fi IP as Metro (never localhost).
EXPO_PUBLIC_API_BASE_URL=
